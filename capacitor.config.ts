import {CapacitorConfig} from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.SkillBlaster.app',
    appName: 'Simplify',
    bundledWebRuntime: false,
    npmClient: 'npm',
    webDir: 'www',
    linuxAndroidStudioPath: '/snap/bin/android-studio',
};

export default config;
